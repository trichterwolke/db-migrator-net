namespace NeosIT.DB_Migrator.Test
{
    using NeosIT.DBMigrator;
    using Xunit;

    public class ProgramTest
    {
        /*
        [Fact]
        public void Integration()
        {
            string username = "faview_build_unittest";
            string password = "LaW8Nv%F";
            string host = "drax.ad.neos-it.de";
            string database = "faview_build_unittest";

           var args = new[] { "--target=mssql", "--host", host, "--username", username, "--password", password, "--database", database, "--strategy=flat migrations,all,true;Fixtures/SimpleMigrations,all,true" };
           Program.Main(args, true);
         
        }
        */
    }
}
